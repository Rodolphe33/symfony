{% extends 'AppBundle::layout.html.twig' %}

{% block main %}
    <h1>Edition d'article</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Modifier" class='waves-effect waves-light btn-small #76ff03' />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('article_index') }}" class='waves-effect waves-light btn-small #76ff03'>Retour</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class='waves-effect waves-light btn-small dark'>
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
