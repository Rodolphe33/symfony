{% extends 'AppBundle::layout.html.twig' %}

{% block main %}
    <h1 class="table  table-borderered">Création d'un nouveau contact</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('contact_index') }}">Retour liste contact</a>
        </li>
    </ul>
{% endblock %}
